CREATE QUERY set_application_fraud_status(VERTEX<Application> application, BOOL fraud_status) { 
  
  start = {application};
  
  start = select s from start:s post-accum s.is_fraud = fraud_status;
  
}
